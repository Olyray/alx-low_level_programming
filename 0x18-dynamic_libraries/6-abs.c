
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
  * _abs - returns the absolute value of an int
  * @c: The int to be checked
  * Return: the absolute value
  */

int _abs(int c)
{
	int value;

	value = abs(c);
	return (value);
}
