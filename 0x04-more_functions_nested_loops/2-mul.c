#include <ctype.h>
#include <stdio.h>

/**
  * mul - checks if a character is a digiti
  * @a: the character to be checked
  * @b: character to be multiplied
  * Return: 1 if it's a digit, 0 if it isn't.
  */

int mul(int a, int b)
{
	int check;

	check = a * b;
	return (check);
}
