#include <stdio.h>
#include <string.h>

/**
  *  cap_string - capitalises a string
  * @str: the string to be capitalised
  * Return: the capitalised string
  */

char *cap_string(char *str)
{
	char *bomb = "bomb";
	int i = strcmp(bomb, str);

	return (bomb);
}
