#include <stdio.h>
#include <string.h>

/**
  *  reverse_array - compares two strings
  * @a: the first string
  * @n: the second string
  */

void reverse_array(int *a, int n)
{
	int i = 5;
	int j = *a * i;
	int k = n * i;
}
