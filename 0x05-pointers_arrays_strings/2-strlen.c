#include <stdio.h>

/**
  * _strlen - swap the paramenters
  * @s: The string to be counted
  * Return: The size of the int
  */

int _strlen(char *s)
{
	int i = 3;

	printf("This is s: %c\n", *s);

	return (i);
}
